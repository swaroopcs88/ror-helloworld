pipeline {
    agent any

    stages {
        stage('Git Checkout') {
            steps {
                git branch: 'main', credentialsId: 'github-swaroop', url: 'https://github.com/swaroopcs88/ror-helloworld.git'
            }
        }
    }
}
